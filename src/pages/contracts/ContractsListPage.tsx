import { FileText } from 'lucide-react'
import { PlaceholderPage } from '../../components/PlaceholderPage'

export function ContractsListPage() {
  return <PlaceholderPage icon={<FileText size={26} strokeWidth={2} />} title="Contracts" />
}
