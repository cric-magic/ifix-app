import { Store } from 'lucide-react'
import { PlaceholderPage } from '../../components/PlaceholderPage'

export function BranchesPage() {
  return <PlaceholderPage icon={<Store size={26} strokeWidth={2} />} title="Branches" />
}
