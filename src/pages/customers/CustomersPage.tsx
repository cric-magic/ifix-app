import { Contact } from 'lucide-react'
import { PlaceholderPage } from '../../components/PlaceholderPage'

export function CustomersPage() {
  return <PlaceholderPage icon={<Contact size={26} strokeWidth={2} />} title="Customers" />
}
